version: 2

updates:
  - package-ecosystem: cargo
    directory: "/"
    schedule:
      interval: weekly
    target-branch: main

    groups:
      rust-dependencies:
        update-types:
          - "minor"
          - "patch"
